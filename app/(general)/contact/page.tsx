import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Contact: Tiux",
    description: "Contact de Tiux",
    keywords: ["Tiux", "Contact de Tiux"],
}

export default function ContactPage(){
  return (
    <span className="text-7xl">Contact de Tiux</span>
  )
}
